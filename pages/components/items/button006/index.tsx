import buttonStyle from './button006.module.css';

const Button006 = () => {
  return (
    <button type="button" className={buttonStyle.button}>
      <span className={buttonStyle.text}>push</span>
    </button>
  );
}

export default Button006;
