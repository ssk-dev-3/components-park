import buttonStyle from './button003.module.css';

const Button003 = () => {
  return (
    <button type='button' className={buttonStyle.button}>
      <p className={buttonStyle.text}>PUSH</p>
    </button>
  );
}

export default Button003;
