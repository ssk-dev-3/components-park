import buttonStyle from './button002.module.css';

const Button002 = () => {
  return (
    <button type='button' className={buttonStyle.button}>
      <p className={buttonStyle.text}>push</p>
    </button>

  );
}

export default Button002;
